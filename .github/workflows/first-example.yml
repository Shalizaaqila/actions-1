name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "Welcome to GitHub Actions!"
      - name: List files
        run: ls
      - name: Read file
        run: cat README.md